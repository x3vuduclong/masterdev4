version: '3'

networks:
  dunghs3:

services:
  nginx:
    image: nginx:stable-alpine
    container_name: "nginx"
    ports:
      - "8080:80"
      - "8000:8000"
    volumes:
      - ./src:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf